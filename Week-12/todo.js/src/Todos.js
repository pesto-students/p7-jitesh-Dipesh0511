import React from 'react'
import { todos } from "./Todo.js"

export default function Todos(props) {
    return ( <
        >
        <
        h3 > Todos List < /h3> <
        Todo todos = { props.todo }
        /> <
        />
    )
}